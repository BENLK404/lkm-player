# Dossier .github

- **ISSUE_TEMPLATE/** : modèles d’issues (bug, feature). Les liens dans `config.yml` pointent vers `BENLK404/lkm-player`.
- **PULL_REQUEST_TEMPLATE.md** : modèle de description pour les pull requests.

Pour activer les **Discussions** : Settings → General → Discussions → Activer.
